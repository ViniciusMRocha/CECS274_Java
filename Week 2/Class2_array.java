import java.util.Scanner;

public class Class2_array {
    public static void main(String [] args) {
    /*

    When you equal an array to a variable all it does it creates are "pointer" another name to the same thing
    To acctualy copy an array you need to use:
        - Manually (for loop)
        - Methods = array.copyOf()
        - system.arrycopy()
        - object.clone()

      int [] expects an array fo values

      create and array >> double [] values = new double [LENGTH];

      Partually filled array
        - You need to keep track of the current size
        - Use a variable that keeps track of the array current size

     Common Array Algorithm

     Tally Counter

         Counter tally = new Counter();
         tally.click();
         tally.click();
         int result = tally.getValue();

         Counter = Object type

         Student Mimi = new Student();
            Student = Object Type of data
            Mimi = Variable name
            New = Create a instance (create an object)
            Student () = Method / Constructor

     Instance Variable
        Access Specifier (Private)



     */


    }
    }


